
import React from 'react';
import { UsersCollection } from '../api/links';

export const Info = () => {
  // const allLinks = UsersCollection.find().fetch();
  // console.log(allLinks)
  return (
    <div>
      <h2>Learn Meteor!</h2>
    </div>
  );
};

 
